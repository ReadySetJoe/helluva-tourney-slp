query getTournament($slug: String!) {
  getTournament(slug: $slug) {
    id
    name
    event {
      id
      name
      sets {
        round
        entrants {
          id
          name
        }
      }
    }
  }
}
